# 🔐 LinkedIn Parser с Ротирующими Прокси

> Полная система для парсинга LinkedIn с использованием ротирующих прокси и управлением сессиями для избежания блокировок.

## ⚡ Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Проверка системы

```bash
python test_proxy_system.py
```

Это покажет, что все компоненты работают:
- ✓ Config
- ✓ RateLimiter
- ✓ ProxyManager
- ✓ SessionManager

### 3. Первый парсинг

```bash
# Парсинг одного профиля
python main.py "https://www.linkedin.com/in/username"

# Парсинг из файла со списком URL
python main.py -f urls.txt
```

## 🎯 Что новое

### ✨ Улучшения системы прокси

| Функция | Раньше | Сейчас |
|---------|--------|--------|
| Источники прокси | 1 (ProxyScrape) | 3+ (ProxyScrape, FreeProxyList, ProxyListPlus) |
| Управление сессиями | ❌ | ✅ Полное управление с 10 параллельными сессиями |
| Ротация User-Agents | ❌ | ✅ 13+ вариантов, автоматическая ротация |
| Управление cookies | ❌ | ✅ Очистка и ротация при ошибках |
| Статистика | Базовая | ✅ Детальная по каждому прокси и сессии |
| Обработка ошибок | Базовая | ✅ Специальные действия для 403, 429, 999 |

### 📊 Новые возможности

```python
# Отслеживание успешности каждого прокси
proxy_manager.mark_success(proxy, response_time=0.75)
proxy_manager.mark_failed(proxy)

# Детальная статистика
proxy_manager.print_stats()  # Красивый вывод топ-10 прокси

# Управление сессиями
session_manager.rotate_session()          # Ротация User-Agent
session_manager.create_new_session_for_ip('192.168.1.1')
session_manager.clear_all_sessions()      # Очистить все

# Получить статистику
stats = proxy_manager.get_stats()
```

## 📁 Структура файлов

```
linkedin-parser/
├── proxy_manager.py          # 🆕 Управление прокси (480+ строк, 3+ источника)
├── session_manager.py        # 🆕 Управление сессиями (300+ строк)
├── linkedin_parser.py        # ✏️ Обновлено: использует SessionManager
├── rate_limiter.py           # ✓ Без изменений
├── config.py                 # ✏️ Обновлено: параметры session
├── main.py                   # ✏️ Обновлено: интеграция всех компонентов
├── test_proxy_system.py      # 🆕 Полное тестирование системы
├── PROXY_SETUP.md            # 🆕 Полная документация
├── requirements.txt          # ✏️ Обновлено: зависимости
└── config.json               # ✓ Конфигурация
```

## 🔧 Конфигурация

### Быстрое парсинг (скорость важнее скрытности)

```json
{
  "rate_limiting": {
    "min_delay": 1,
    "max_delay": 2
  },
  "proxy": {
    "max_proxies": 100
  },
  "session": {
    "max_sessions": 20
  }
}
```

### Безопасное парсинг (скрытность важнее скорости)

```json
{
  "rate_limiting": {
    "min_delay": 10,
    "max_delay": 20
  },
  "proxy": {
    "max_proxies": 30
  },
  "session": {
    "max_sessions": 5
  }
}
```

## 📈 Примеры использования

### Пример 1: Базовое парсинг

```python
from config import Config
from proxy_manager import ProxyManager
from rate_limiter import RateLimiter
from session_manager import SessionManager
from linkedin_parser import LinkedInParser

config = Config()
proxy_manager = ProxyManager(config)
rate_limiter = RateLimiter()
session_manager = SessionManager(config)

parser = LinkedInParser(config, proxy_manager, rate_limiter, session_manager)
profile = parser.parse_profile('https://www.linkedin.com/in/elon-musk')

# Показать статистику
proxy_manager.print_stats()
```

### Пример 2: Парсинг с обработкой ошибок

```python
try:
    parser = LinkedInParser(config, proxy_manager, rate_limiter, session_manager)
    profile = parser.parse_profile(url)
    
    if profile:
        print("✓ Профиль успешно загружен")
    else:
        print("✗ Не удалось загрузить профиль")
        
except Exception as e:
    print(f"✗ Ошибка: {e}")

# Всегда показать статистику
finally:
    proxy_manager.print_stats()
    session_stats = session_manager.get_stats()
    print(f"Активных сессий: {session_stats['total_sessions']}")
```

### Пример 3: Параллельный парсинг нескольких профилей

```python
import concurrent.futures

urls = [
    'https://www.linkedin.com/in/bill-gates',
    'https://www.linkedin.com/in/steve-jobs',
    'https://www.linkedin.com/in/mark-zuckerberg',
]

def parse_url(url):
    config = Config()
    proxy_manager = ProxyManager(config)
    rate_limiter = RateLimiter()
    session_manager = SessionManager(config)
    parser = LinkedInParser(config, proxy_manager, rate_limiter, session_manager)
    return parser.parse_profile(url)

with concurrent.futures.ThreadPoolExecutor(max_workers=3) as executor:
    results = list(executor.map(parse_url, urls))

print(f"Успешно загружено: {sum(1 for r in results if r)}/{len(urls)}")
```

## 🚨 Решение проблем

### LinkedIn все еще блокирует

1. **Увеличить задержки:**
   ```json
   {
     "rate_limiting": {
       "min_delay": 15,
       "max_delay": 30
     }
   }
   ```

2. **Уменьшить max_sessions:**
   ```json
   {
     "session": {
       "max_sessions": 3
     }
   }
   ```

3. **Проверить прокси:**
   ```bash
   python test_proxy_system.py
   ```

### Прокси не работают

1. **Проверить список прокси:**
   ```python
   proxy_manager = ProxyManager(config)
   print(f"Доступных прокси: {len(proxy_manager.proxies)}")
   print(proxy_manager.proxies[:3])  # Первые 3 прокси
   ```

2. **Обновить список:**
   ```python
   proxy_manager._fetch_proxies()
   proxy_manager.print_stats()
   ```

## 📊 Отслеживание прогресса

### Вывод статистики

```
============================================================
СТАТИСТИКА ИСПОЛЬЗОВАНИЯ ПРОКСИ
============================================================
Всего запросов: 156
Успешных: 144
Ошибок: 12
Успешность: 92.3%
Доступных прокси: 47
Отказавших прокси: 3
Последнее обновление: 2025-12-24T10:15:23.456789
------------------------------------------------------------
Статистика по каждому прокси:
  123.45.67.89:8080: 45 запросов, успех 44/1, 97.8%, avg response: 0.82s
  98.76.54.32:3128: 42 запросов, успех 40/2, 95.2%, avg response: 0.95s
  ...
============================================================
```

## 🔒 Безопасность и анонимность

### Что защищает система:

✅ **IP маскировка:**
- Использование прокси с разными IP
- X-Forwarded-For заголовок в сессиях
- Автоматическая ротация неработающих IP

✅ **User-Agent ротация:**
- 13+ реальных User-Agents
- 20% вероятность обновления при каждом запросе
- Разные агенты для разных сессий

✅ **Cookie управление:**
- Очистка cookies при блокировке (403)
- Полная очистка при лимитах (429)
- Отдельные cookies для разных сессий

✅ **Rate limiting:**
- Случайные задержки между запросами
- Экспоненциальная задержка при ошибках
- Адаптивное ожидание при 429

## 📚 Документация

Полная документация доступна в [PROXY_SETUP.md](PROXY_SETUP.md):
- Детальное описание каждого компонента
- API справка
- Примеры использования
- Оптимизация и настройка
- Решение проблем

## 🔄 Интеграция с системами мониторинга

```python
import json
from datetime import datetime

# Экспортировать метрики
metrics = {
    'timestamp': datetime.now().isoformat(),
    'proxy': proxy_manager.get_stats(),
    'session': session_manager.get_stats(),
    'uptime': 'OK'
}

with open('metrics.json', 'w') as f:
    json.dump(metrics, f, indent=2)
```

## 🤝 Способ использования

### Для разработки
```bash
# Установить в режиме разработки
pip install -e .

# Запустить тесты
python test_proxy_system.py
```

### Для продакшена
```bash
# Использовать с superviser для работы 24/7
# Или запустить как фоновую задачу cron
```

## ⚠️ Важно

- **Используйте ответственно:** LinkedIn имеет Terms of Service
- **Лимиты:** Используйте разумные задержки (минимум 5-10 секунд между запросами)
- **Прокси:** Выбирайте надежные источники прокси
- **Мониторинг:** Отслеживайте rate limits и блокировки

## 📝 Лицензия

Это учебный проект. Используйте в соответствии с законодательством.

## 🤖 Автоматизация

### Парсинг каждый час
```bash
0 * * * * cd /path/to/project && python main.py -f urls.txt -o results
```

### Парсинг каждый день в 3:00
```bash
0 3 * * * cd /path/to/project && python main.py -f urls.txt -o daily_results
```

## 🎓 Что вы научитесь

- ✓ Управлению ротирующимися прокси
- ✓ Управлению сессиями и cookies
- ✓ Обработке различных HTTP ошибок
- ✓ Rate limiting и задержкам
- ✓ Сбору и анализу статистики
- ✓ Обходу защиты от автоматизации

## 📞 Поддержка

Если вы столкнулись с проблемами:

1. **Проверить логи:** `linkedin_parser.log`
2. **Запустить тесты:** `python test_proxy_system.py`
3. **Проверить конфигурацию:** `config.json`
4. **Обновить прокси:** Вручную вызвать `_fetch_proxies()`

---

**Версия:** 2.0 (с полной системой прокси и сессий)
**Последнее обновление:** 2025-12-24
**Статус:** ✓ Полностью протестирована
